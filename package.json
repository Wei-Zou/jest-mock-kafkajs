{
  "name": "jest-mock-kafkajs",
  "version": "1.0.0",
  "description": "Mock kafkajs using jest manual mock",
  "main": "index.js",
  "scripts": {
    "test": "npm run setup-local-env && jest",
    "test-actual": "npm run setup-local-env && jest ./test/kafka-connection-actual.spec.js",
    "test-mocked": "npm run setup-local-env && jest ./test/kafka-connection-mock.spec.js",
    "setup-local-env": "docker-compose run --rm start_dependencies"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Wei-Zou/jest-mock-kafkajs.git"
  },
  "author": "Wei Zou",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Wei-Zou/jest-mock-kafkajs/issues"
  },
  "homepage": "https://github.com/Wei-Zou/jest-mock-kafkajs#readme",
  "dependencies": {
    "kafkajs": "1.8.0"
  },
  "devDependencies": {
    "jest": "24.8.0"
  }
}
